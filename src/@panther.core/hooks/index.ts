/** Barrel for the shared hooks. */

export { useColorScheme, readStoredColorScheme } from '@/@panther.core/hooks/useColorScheme'
export {
  COLOR_SCHEME_STORAGE_KEY,
  DEFAULT_COLOR_SCHEME,
} from '@/@panther.core/hooks/useColorScheme'
export type {
  ColorScheme,
  UseColorSchemeOptions,
  UseColorSchemeResult,
} from '@/@panther.core/hooks/useColorScheme'
export { useElementSize } from '@/@panther.core/hooks/useElementSize'
export type { ElementSize } from '@/@panther.core/hooks/useElementSize'
export { useHashTarget } from '@/@panther.core/hooks/useHashTarget'
export type { HashTarget, UseHashTargetOptions } from '@/@panther.core/hooks/useHashTarget'
